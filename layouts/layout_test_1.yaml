# comment
kind: layout
description: "layout for ..."

table:
  hide:
    resource-types:
      #- aws_instance
      - "aws_bucket"
      - aws_vpc
      - "AWS::BUCKET"
      - null_resource
      #- "*"  # hide all
